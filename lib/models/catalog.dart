class Item {
  // immutable can olny change via construtor
  final String id;
  final String name;
  final String dedc;
  final num price;
  final String color;
  final String image;

  Item({this.id, this.name, this.dedc, this.price, this.color, this.image});
}
